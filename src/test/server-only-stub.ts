// Stub para o import "server-only" ao rodar sob Vitest (fora do bundler do
// Next.js, que normalmente resolve esse pacote sozinho). Sem efeito em
// produção — só usado via alias no vitest.config.ts.
export {}
